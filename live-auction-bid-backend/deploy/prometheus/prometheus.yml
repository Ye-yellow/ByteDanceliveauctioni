global:
  scrape_interval: 5s
  evaluation_interval: 5s

scrape_configs:
  - job_name: live-auction-backend
    metrics_path: /metrics
    static_configs:
      - targets:
          - auction-backend:18080
