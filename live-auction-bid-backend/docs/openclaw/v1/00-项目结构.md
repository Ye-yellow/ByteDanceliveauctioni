# V1 项目结构占位

当前阶段先做需求，不恢复具体实现文件。

但后端项目仍保留目标架构目录，方便后续按步骤补代码：

```text
api/
├── auction/service/v1/
└── ai/service/v1/

app/auction/service/
├── cmd/server/
├── configs/
└── internal/
    ├── conf/
    ├── server/
    ├── service/
    ├── biz/
    ├── data/
    └── realtime/

deploy/
openapi/
scripts/
third_party/
```

说明：

- 目录通过 `.gitkeep` 保留；
- 不代表对应模块已经实现；
- 后续每一步实现都先过需求/设计确认，再提交代码。
